.env
# Python cache
__pycache__/
*.pyc
*.pyo

# Raw data — large file, regenerated by scraper.py
data/raw/github_users.csv
data/processed/github_features.csv
.vscode/

# model.pkl is intentionally NOT ignored — it must be committed
# so docker-compose up works without retraining